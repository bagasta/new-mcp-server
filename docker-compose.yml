services:
  mcp-server:
    build: .
    container_name: mcp-server
    restart: always
    ports:
      - "8190:8190"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./assets:/app/assets
